#include <stdio.h>

int main(){
	struct pokemon{
		char name[100];
		int Lv;
		int Hp;
	};
	
	int n;

	struct pokemon a;

	scanf("%s %d %d", a.name, &a.Lv, &a.Hp);
	scanf("%s %d %d", b.name, &b.Lv, &b.Hp);
	scanf("%s %d %d", c.name, &c.Lv, &c.Hp);
	scanf("%d", &n);

	for(int i=0; i< 3; i++){
		if(n > ){
			printf("");
		}
		
	}
	return 0;
}
